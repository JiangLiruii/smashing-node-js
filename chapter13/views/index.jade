extends layout
block body
h2 ALl items
if (items.length)
  ul
    each item in items
      li 
        h3: a(href="/item/#{item.id}") = item.title
        = item.description
else 
  p No items in cart
h2 create new item

form(action="/create", method="POST")
  p
    label Title
    input(type="text", name="title")
  p
    label description
    textarea(name="desctiption")
  p
    button Submit